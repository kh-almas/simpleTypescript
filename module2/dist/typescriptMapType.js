"use strict";
{
    const numArray = [1, 2, 3];
    // const strArray :string[] = ['1', '2', '3'];
    const convertArrType = numArray.map((number) => number.toString());
    console.log(convertArrType);
    const checkAreaST = {
        height: "100",
        width: 40,
    };
}
